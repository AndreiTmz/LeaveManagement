## todo: update readmne when project is done

## use docker-compose up -d --build
## if you want to start fresh (new db, fresh seed, use 'docker-compose down -v' and then 'docker-compose up -d --build')

# IMPORTANT
## use 'docker-compose down' if you want to keep the database and the data after removing the container
## or use 'docker-compose stop'